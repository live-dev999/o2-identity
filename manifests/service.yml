apiVersion: v1
kind: Service
metadata:
    name: "o2-identity-api"
    labels:
        app: "o2-identity-api"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "o2-identity-api"