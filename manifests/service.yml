apiVersion: v1
kind: Service
metadata:
    name: "o2-752d"
    labels:
        app: "o2-752d"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "o2-752d"