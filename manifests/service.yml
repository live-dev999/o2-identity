apiVersion: v1
kind: Service
metadata:
    name: "o2-identity-web"
    labels:
        app: "o2-identity-web"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "o2-identity-web"