apiVersion: v1
kind: Service
metadata:
    name: "o2aks-c512"
    labels:
        app: "o2aks-c512"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "o2aks-c512"