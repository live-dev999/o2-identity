apiVersion: v1
kind: Service
metadata:
    name: "o2-identity-web-svc"
    labels:
        app: "o2-identity-web"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "o2-identity-web"