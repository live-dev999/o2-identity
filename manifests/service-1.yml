apiVersion: v1
kind: Service
metadata:
    name: "o2platformcluster-397a"
    labels:
        app: "o2platformcluster-397a"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "o2platformcluster-397a"